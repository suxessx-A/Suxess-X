# Required runtime
PORT=3000
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# OpenAI (coaching generation)
AI_INTEGRATIONS_OPENAI_BASE_URL=
AI_INTEGRATIONS_OPENAI_API_KEY=

# Stripe (billing + webhook signature verification)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Magic-link auth (v1.2+)
# RESEND_API_KEY: from https://resend.com — sender domain must be verified
RESEND_API_KEY=re_...
# SESSION_SECRET: 32+ byte random string used to sign session JWTs.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
SESSION_SECRET=
# APP_DEEP_LINK_BASE: deep-link prefix the magic-link button opens on the
# device. Must match the iOS / Android app URL scheme.
APP_DEEP_LINK_BASE=amplifyx://verify

# Apple In-App Purchase (v1.3+)
# APPLE_SHARED_SECRET: shared secret from App Store Connect (My Apps > app >
# App Information > App-Specific Shared Secret). Required by POST
# /api/auth/apple-receipt to call the legacy verifyReceipt endpoint.
APPLE_SHARED_SECRET=
# APPLE_BUNDLE_ID: iOS bundle identifier. Not strictly required for the
# legacy verifyReceipt flow, but reserved for future App Store Server
# Notifications V2 signature verification.
APPLE_BUNDLE_ID=co.amplify-x.momentum
