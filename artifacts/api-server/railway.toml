[build]
builder = "NIXPACKS"
buildCommand = "npm install --ignore-scripts && node build.mjs"

[deploy]
startCommand = "node dist/index.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
