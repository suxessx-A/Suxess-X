Please make these six fixes to the app. Do not change anything else.

FIX 1 — Refine My Situation must update the display
In CoachingResultCard.tsx find where RefineMySituation is rendered. The onRefined callback receives a new coaching result. Make it call setResult(newResult) so the refined coaching replaces the current result on screen. The user must see new content appear after refining.

FIX 2 — Role shift text wrapping
In CoachingResultCard.tsx find the roleShift display row. Add flexWrap: "wrap" to the row container and flexShrink: 1 to each text element so text wraps instead of being cut off on smaller screens.

FIX 3 — Execution Loop language sharpened
In RefineAndExecutionLoop.tsx find the costText in the commitment section and replace:
"Every moment you delay this costs more than you think. The Captain commits. The Passenger waits."
With:
"Right now you are in Passenger mode — waiting instead of acting. Every time you delay this specific move, you reinforce the pattern keeping you from your goal. The Captain does not wait to feel ready. They commit. Then act within 5 seconds."

Find the blocker cost text and replace:
"Every time you delay this specific action, you reinforce the pattern that keeps you from where you said you want to be in 6 months."
With:
"Naming what blocked you is the Captain move. The Passenger avoids and explains. The Captain identifies the obstacle and removes it. Your next move is to remove this specific blocker before tomorrow."

FIX 4 — Connect action to 6-month goal in all prompts
In artifacts/api-server/src/routes/coaching.ts find the STYLE_RULES constant and add this line at the end:
"- behavioralObjective MUST end with one sentence connecting the action to the user's 6-month goal. Format: This moves you toward: [restate their goal in active present tense]. If no goal is provided omit this sentence."

FIX 5 — Premium lock with upgrade prompt
In CoachingResultCard.tsx find where sections with premium: true are rendered. Replace the current lock display with a TouchableOpacity that calls Linking.openURL("https://buy.stripe.com/aFa8wReBd99VgpR8HO5kk00") styled with a gold background, showing: lock emoji, section title, and text "Unlock with Premium — $20/month or $6/week".

FIX 6 — Free tier: 3 flows, one run each
In CoachingContext.tsx or wherever flows are initiated, add this logic using AsyncStorage:

1. Read "suxess_sessions" from AsyncStorage to get an array of completed sessions
2. Read "suxess_premium" — if this equals "true" the user is premium and bypass all checks
3. If not premium, count how many DISTINCT flowTypes exist in the sessions array
4. If a user tries to start a flow they have already completed (same flowType exists in sessions), show a modal saying: "You have already used this flow on the free tier. Upgrade to Premium for unlimited runs across all 6 flows." with buttons linking to Stripe weekly (https://buy.stripe.com/4gM6oJ3WzgCn1uXaPW5kk01) and monthly (https://buy.stripe.com/aFa8wReBd99VgpR8HO5kk00)
5. If the user has completed 3 distinct flows and tries to start a 4th new one, show a modal saying: "You have completed your 3 free flows. Upgrade to Premium to access all 6 flows with unlimited runs." with the same Stripe buttons
6. If the user has completed fewer than 3 distinct flows and the flow is new, allow it to proceed normally

Then rebuild and restart.